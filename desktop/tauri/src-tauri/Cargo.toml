[package]
name = "quantagent_macos"
version = "0.1.0"
description = "QuantAgent macOS desktop shell"
authors = ["QuantAgent"]
edition = "2021"

[lib]
name = "quantagent_macos_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
