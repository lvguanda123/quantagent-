[package]
name = "quantagent_academic"
version = "0.1.0"
description = "QuantAgent Academic desktop shell (高校版)"
authors = ["QuantAgent"]
edition = "2021"

[lib]
name = "quantagent_academic_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
