[package]
name = "quantagent_trader"
version = "0.1.0"
description = "QuantAgent Trader desktop shell (企业版)"
authors = ["QuantAgent"]
edition = "2021"

[lib]
name = "quantagent_trader_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
